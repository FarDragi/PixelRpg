# Migration `20201128025740-add-player`

This migration has been generated by Gabriel-Paulucci at 11/27/2020, 11:57:40 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `Player` (
    `id` INT NOT NULL AUTO_INCREMENT,
    `userId` INT NOT NULL,
    `guildId` INT NOT NULL,
UNIQUE INDEX `Player.guildId_userId_unique`(`guildId`,
`userId`),

    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `Guild` (
    `id` INT NOT NULL AUTO_INCREMENT,
    `discordId` VARCHAR(191) NOT NULL,
    `configId` INT NOT NULL,
UNIQUE INDEX `Guild.discordId_unique`(`discordId`),
UNIQUE INDEX `Guild.configId_unique`(`configId`),

    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `User` (
    `id` INT NOT NULL AUTO_INCREMENT,
    `discordId` VARCHAR(191) NOT NULL,
UNIQUE INDEX `User.discordId_unique`(`discordId`),

    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `GuildConfig` (
    `id` INT NOT NULL AUTO_INCREMENT,
    `prefix` VARCHAR(191) NOT NULL,

    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

ALTER TABLE `Player` ADD FOREIGN KEY (`userId`) REFERENCES `User`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `Player` ADD FOREIGN KEY (`guildId`) REFERENCES `Guild`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `Guild` ADD FOREIGN KEY (`configId`) REFERENCES `GuildConfig`(`id`) ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201128023950-add-guild-config..20201128025740-add-player
--- datamodel.dml
+++ datamodel.dml
@@ -2,25 +2,37 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
     provider = "mysql"
-    url = "***"
+    url = "***"
 }
 generator client {
     provider = "prisma-client-js"
 }
+model Player {
+    id      Int   @id @default(autoincrement())
+    userId  Int
+    guildId Int
+    user    User  @relation(fields: [userId], references: [id])
+    guild   Guild @relation(fields: [guildId], references: [id])
+
+    @@unique([guildId, userId])
+}
+
 model Guild {
     id        Int         @id @default(autoincrement())
     discordId String      @unique
     configId  Int         @unique
     config    GuildConfig @relation(fields: [configId], references: [id])
+    Player    Player[]
 }
 model User {
-    id        Int    @id @default(autoincrement())
-    discordId String @unique
+    id        Int      @id @default(autoincrement())
+    discordId String   @unique
+    Player    Player[]
 }
 model GuildConfig {
     id     Int     @id @default(autoincrement())
```


